# macOS + DOTFILES_PROFILE=work only. Sourced by .zshrc after darwin.zsh.
# Put non-secret, work-only config here (aliases, env vars, tool defaults).
# Actual credentials/tokens still belong in a gitignored *.local.zsh file.
