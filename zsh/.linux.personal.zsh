# Linux + DOTFILES_PROFILE=personal only. Sourced by .zshrc after linux.zsh.
# Put non-secret, personal-only config here (aliases, env vars, tool defaults).
[[ -d /usr/local/go/bin ]] && export PATH="/usr/local/go/bin:$PATH"
